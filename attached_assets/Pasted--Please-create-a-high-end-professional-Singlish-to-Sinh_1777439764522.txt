"Please create a high-end, professional Singlish to Sinhala real-time converter website named 'SPsinhalish' from scratch. Here are the total requirements to be implemented at once:

1. Core Phonetic Engine:

Real-time conversion from Singlish (English phonetics) to Sinhala Unicode.

Must follow standard phonetic rules (e.g., 'ka' -> 'ක', 'nda' -> 'ඳ', 'sh' -> 'ශ').

2. Dual-Format Support (Unicode & Legacy FM):

Add a toggle switch or dropdown to choose between 'Unicode Mode' and 'Legacy (FM-Abhaya) Mode'.

In Legacy Mode, the converter must instantly map Singlish input to the ASCII character set used by FM-Abhaya fonts. This allows users to copy the text, paste it into MS Word, and simply apply the 'FM-Abhaya' font.

3. AI-Enhanced Creative Word Export:

Add a button: 'Download as Creative Word Document (.docx)'.

Use Python's python-docx library to generate a professionally designed document.

Requirements for the Word file: >    - Add a professional Header with 'SPsinhalish' branding.

Add a Footer with page numbers and a 'Generated by SPsinhalish' tag.

Use creative styling (e.g., a blue/gray themed border or a modern title block) so the exported file looks like a high-quality report or typeset assignment.

4. Offline Capability (Progressive Web App - PWA):

Generate a manifest.json and a robust service-worker.js.

The app must be 'Installable' on PC/Mobile and function completely offline once cached.

5. User Interface (UI/UX):

Design: A sleek, modern 'Developer-style' dark/light theme.

Functional Buttons: 'Copy to Clipboard', 'Clear Text', 'Download .txt'.

Help Guide: A 'Typing Guide' button that opens a popup showing the key-map.

Responsive: Must be perfectly usable on mobile phones and desktops.

6. Technical Setup:

Use HTML, CSS, and Vanilla JS for the conversion engine (for speed).

Use a Python Flask backend specifically for the creative .docx generation.

Ensure the .replit file is configured with the correct 'run' command (python main.py) on port 5000. No login/auth required."